# 树莓派 / Debian 上不要直接运行: pip3 install -r requirements.txt
# 会报 externally-managed-environment（PEP 668）。
#
# 若 stage4/main 已能运行 → 无需再装（系统里已有 numpy 等）。
#
# 需要新装依赖时，任选其一：
#   sudo apt install python3-numpy python3-yaml
#   bash setup_venv.sh    # 在项目 .venv 里 pip 安装全部依赖
#
pymavlink>=2.4.0
PyYAML>=5.1
numpy>=1.20.0
